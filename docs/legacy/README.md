# Legacy Documents

This folder stores historical context documents that informed the current implementation.

- `project_onboarding_guide_objective_free_alife.md`: original proposal
- `unified_review.md`: review and rationale during spec evolution

These files are archival references only. Use `spec.md` in the repository root as the implementation source of truth.
