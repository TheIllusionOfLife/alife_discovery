# PR26 Follow-Up Canonical Bundle

This directory is the tracked lightweight bundle for PR26 follow-up outputs.

- Keep only compact reproducibility artifacts here:
  - `manifest.json`
  - `checksums.sha256`
  - summary JSON/CSV files consumed by paper scripts
- Store heavy/raw artifacts in Zenodo and record DOI + file metadata in
  `manifest.json`.
- Current published archive DOI: `10.5281/zenodo.18713158`
- Current tracked bundle was generated with full follow-up settings
  (`n_rules=5000`, `steps=200`).
